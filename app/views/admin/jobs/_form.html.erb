<div class="container">
          <!-- Horizontal Form -->
          <div class="box box-info">
            <div class="box-header with-border">
              <h3 class="box-title">Job Form</h3>
            </div>
            <!-- /.box-header -->
            <!-- form start -->

            <div class="form-horizontal">
            <%= form_with model: @job, url: {action: @action},local: true do |form| %>   
              <div class="box-body">

                <div class="form-group">
                  <%= form.label :job_image, class:"col-sm-2 control-label" %>
                  <div class="col-sm-10">
                    <%= form.file_field :job_image,:class => "form-control",placeholder:"Image"%>
                    <% if @job.job_image? %>
                        <%= image_tag @job.job_image.url(:thumb) %>
                    <% end %>
                    <% if @job.errors.any? %>
                      <div id="error_explanation" >
                        <ul class="field_with_errors">
                          <% @job.errors.full_messages_for(:job_image).each do |msg| %>
                            <li><%= msg %></li>
                          <% end %>
                        </ul>
                      </div>
                    <% end %>
                  </div>
                </div>


                <div class="form-group">
                  <%= form.label :title, class:"col-sm-2 control-label" %>
                  <div class="col-sm-10">
                    <%= form.text_field :title,:class => "form-control",placeholder:"Title"%>
                    <% if @job.errors.any? %>
                      <div id="error_explanation" >
                        <ul class="field_with_errors">
                          <% @job.errors.full_messages_for(:title).each do |msg| %>
                            <li><%= msg %></li>
                          <% end %>
                        </ul>
                      </div>
                    <% end %>
                  </div>
                </div>

                <div class="form-group">
                  <%= form.label :url, class:"col-sm-2 control-label" %>
                  <div class="col-sm-10">
                    <%= form.text_field :url,:class => "form-control",placeholder:"Url"%>
                    <% if @job.errors.any? %>
                      <div id="error_explanation" >
                        <ul class="field_with_errors">
                          <% @job.errors.full_messages_for(:title).each do |msg| %>
                            <li><%= msg %></li>
                          <% end %>
                        </ul>
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
              <!-- /.box-body -->
              <div class="box-footer">
                <%= link_to 'Cancel', admin_jobs_path, class: "btn btn-default" %>
                <%= form.submit class: "btn btn-info pull-right" %>
              </div>
            <% end %>
          </div>
        </div>
</div>