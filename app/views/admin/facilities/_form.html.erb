<div class="container">
          <!-- Horizontal Form -->
          <div class="box box-info">
            <div class="box-header with-border">
              <h3 class="box-title">New Facility</h3>
            </div>
            <!-- /.box-header -->
            <!-- form start -->

            <div class="form-horizontal">
            <%= form_with model: @facility, url: {action: @action},local: true do |form| %>   
              <div class="box-body">
                <div class="form-group">
                  <%= form.label :title, class:"col-sm-2 control-label" %>

                  <div class="col-sm-10">
                    <%= form.text_field :title,:class => "form-control",placeholder:"Title"%>
                    <% if @facility.errors.any? %>
                      <div id="error_explanation" >
                        <ul class="field_with_errors">
                          <% @facility.errors.full_messages_for(:title).each do |msg| %>
                            <li><%= msg %></li>
                          <% end %>
                        </ul>
                      </div>
                    <% end %>
                  </div>
                </div>

                <div class="form-group">
                  <%= form.label :icon_class, class:"col-sm-2 control-label" %>
                  <div class="col-sm-10">
                    <%= form.text_field :icon_class,:class => "form-control",placeholder:"Url"%>
                    <% if @facility.errors.any? %>
                      <div id="error_explanation" >
                        <ul class="field_with_errors">
                          <% @facility.errors.full_messages_for(:icon_class).each do |msg| %>
                            <li><%= msg %></li>
                          <% end %>
                        </ul>
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
              <!-- /.box-body -->
              <div class="box-footer">
                <%= link_to 'Cancel', admin_facilities_path, class: "btn btn-default" %>
                <%= form.submit class: "btn btn-info pull-right" %>
              </div>
            <% end %>
          </div>
        </div>
</div>